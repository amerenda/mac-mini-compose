# Smart Lighting v2 — Per-room "use defaults" toggles
# When ON (default), the room uses the global default scene template.
# When OFF, the room keeps its custom scene (edited via HA scene editor).
sl_living_room_use_defaults:
  name: "SL: Living Room Use Defaults"
  icon: mdi:content-copy
sl_bedroom_use_defaults:
  name: "SL: Bedroom Use Defaults"
  icon: mdi:content-copy
sl_bathroom_use_defaults:
  name: "SL: Bathroom Use Defaults"
  icon: mdi:content-copy
sl_kitchen_use_defaults:
  name: "SL: Kitchen Use Defaults"
  icon: mdi:content-copy
sl_hallway_use_defaults:
  name: "SL: Hallway Use Defaults"
  icon: mdi:content-copy
