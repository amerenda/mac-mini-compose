# Smart Lighting v2 — Living Room motion trigger
# Stub: update entity_id when motion sensor is installed
alias: "SL: Living Room — Motion"
id: sl_living_room_motion
trigger:
  - platform: state
    entity_id: binary_sensor.living_room_motion
    to: "on"
condition:
  - condition: state
    entity_id: input_boolean.sl_enabled
    state: "on"
  - condition: template
    value_template: >
      {% set window = states('sensor.sl_current_window') %}
      {{ is_state('input_boolean.sl_living_room_motion_' ~ window, 'on') }}
action:
  - service: script.sl_room_on
    data:
      room: living_room
  - service: timer.start
    target:
      entity_id: timer.sl_living_room_motion_off
    data:
      duration: >
        {{ states('input_number.sl_living_room_motion_timeout') | int * 60 }}
mode: restart
