# Hallway has two switches — both control the same room
alias: "SL: Hallway — Switch 1"
id: sl_hallway_switch_1
use_blueprint:
  path: _defaults/sl_hue_dimmer.yaml
  input:
    switch_action_topic: zigbee2mqtt/hallway_s_1/action
    room: hallway
    light_entity: light.hallway
    min_brightness_pct: 20
    brightness_step_pct: 20
