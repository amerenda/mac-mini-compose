# PARKED — not loaded by ResourceSync (only resource-sync/stacks.toml is).
# After murderbot disk migration: copy this block back into stacks.toml (or merge file).
# Last parked from main alongside removal of live [[stack]] media-server.

[[stack]]
name = "media-server"
description = "Jellyfin, radarr/sonarr/bazarr/prowlarr/profilarr, sabnzbd, nginx/certbot, DO dyndns"
tags = ["murderbot", "media"]
deploy = true
[stack.config]
server = "murderbot"
repo = "amerenda/komodo-dean-gitops"
branch = "main"
file_paths = ["murderbot/media-server/compose.yaml"]
git_account = "amerenda"
[stack.config.pre_deploy]
command = """
export BWS_ACCESS_TOKEN=$(cat /run/secrets/bws-access-token)
bash murderbot/media-server/pre-deploy.sh
"""
