# Secrets — never commit
.env

# mac-mini-m4 stack secrets (written by pre_deploy)
mac-mini-m4/core/.env
mac-mini-m4/automation/.env
mac-mini-m4/monitoring/.env
mac-mini-m4/monitoring/ha-token
mac-mini-m4/runners/compose.env
mac-mini-m4/llm/.env
mac-mini-m4/llm/.ansible-memory-bytes
mac-mini-m4/llm/ollama.env
mac-mini-m4/bind9/keys/key.conf
mac-mini-m4/komodo/secrets/

# archlinux stack secrets (written by pre_deploy)
archlinux/media-server/.env
archlinux/media-server/vpn-user.txt
archlinux/media-server/vpn-password.txt

# archlinux Periphery (deployed by Ansible) — passkey rendered from compose.env.example
archlinux/komodo/compose.env

# Docker volumes (local data)
data/

# macOS
.DS_Store

# Editor
*.swp
*.swo
*~

# Local tooling (dashboard extraction)
.venv-dash-extract/
