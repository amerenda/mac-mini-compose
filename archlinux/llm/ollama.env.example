# Optional: Ollama server tunables — same idea as mac-mini-m4; on Linux the agent
# writes llm/ollama.env but UI-driven plist merge is Mac-only. Use
# archlinux/ollama/environment (OLLAMA_HOST=…) or edit systemd drop-in if needed.
