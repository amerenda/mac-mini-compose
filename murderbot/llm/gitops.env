# Merged into murderbot/llm/.env on every Komodo deploy (see llm/pre-deploy.sh).
#
# LAN IP of murderbot (llm-manager backend reaches agents here).
AGENT_ADDRESS=https://10.100.20.19:8090

# Optional: pin agent image when you cannot wait for target-version resolution:
# AGENT_IMAGE_TAG=sha-abc1234
