alias: "SL: Bedroom Switch"
description: "Hue Dimmer bedroom_s_1 → SL scripts (MQTT trigger, Z2M-paired)"
mode: restart
max_exceeded: silent

trigger:
  - platform: mqtt
    topic: zigbee2mqtt/bedroom_s_1
    value_template: "{{ value_json.action }}"
    payload: "on_press_release"
    id: btn1_short
  - platform: mqtt
    topic: zigbee2mqtt/bedroom_s_1
    value_template: "{{ value_json.action }}"
    payload: "on_hold"
    id: btn1_long
  - platform: mqtt
    topic: zigbee2mqtt/bedroom_s_1
    value_template: "{{ value_json.action }}"
    payload: "up_press_release"
    id: btn2_short
  - platform: mqtt
    topic: zigbee2mqtt/bedroom_s_1
    value_template: "{{ value_json.action }}"
    payload: "up_hold"
    id: btn2_long
  - platform: mqtt
    topic: zigbee2mqtt/bedroom_s_1
    value_template: "{{ value_json.action }}"
    payload: "down_press_release"
    id: btn3_short
  - platform: mqtt
    topic: zigbee2mqtt/bedroom_s_1
    value_template: "{{ value_json.action }}"
    payload: "down_hold"
    id: btn3_long
  - platform: mqtt
    topic: zigbee2mqtt/bedroom_s_1
    value_template: "{{ value_json.action }}"
    payload: "off_press_release"
    id: btn4_short
  - platform: mqtt
    topic: zigbee2mqtt/bedroom_s_1
    value_template: "{{ value_json.action }}"
    payload: "off_hold"
    id: btn4_long

action:
  - choose:
      - conditions: "{{ trigger.id == 'btn1_short' }}"
        sequence:
          - if:
              - condition: state
                entity_id: light.bedroom
                state: "on"
            then:
              - service: script.sl_room_off
                data:
                  room: bedroom
            else:
              - service: script.sl_room_on
                data:
                  room: bedroom
      - conditions: "{{ trigger.id == 'btn1_long' }}"
        sequence:
          - service: script.sl_room_off
            data:
              room: bedroom
      - conditions: "{{ trigger.id == 'btn2_short' }}"
        sequence:
          - service: light.turn_on
            target:
              entity_id: light.bedroom
            data:
              brightness_step_pct: 20
      - conditions: "{{ trigger.id == 'btn2_long' }}"
        sequence:
          - service: light.turn_on
            target:
              entity_id: light.bedroom
            data:
              brightness_pct: 100
      - conditions: "{{ trigger.id == 'btn3_short' }}"
        sequence:
          - service: light.turn_on
            target:
              entity_id: light.bedroom
            data:
              brightness_step_pct: -20
      - conditions: "{{ trigger.id == 'btn3_long' }}"
        sequence:
          - service: light.turn_on
            target:
              entity_id: light.bedroom
            data:
              brightness_pct: 5
      - conditions: "{{ trigger.id == 'btn4_short' }}"
        sequence:
          - service: script.sl_cycle_scene
            data:
              room: bedroom
      - conditions: "{{ trigger.id == 'btn4_long' }}"
        sequence:
          - service: script.sl_toggle_all
