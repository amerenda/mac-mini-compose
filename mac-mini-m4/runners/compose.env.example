## GitHub Actions Runner Environment
## Copy to compose.env and fill in values.
## Secrets are read from /run/secrets/ by entrypoint-wrapper.sh — do NOT put secrets here.

## DockerHub
DOCKERHUB_USERNAME=amerenda

## Runner config
RUNNER_SCOPE=repo
## EPHEMERAL=true  # Disabled — ephemeral runners miss queued jobs during restart cycle
RUNNER_WORKDIR=/_work
RANDOM_RUNNER_SUFFIX=false
