# Route internal domains to BIND9 (Docker internal network)
# BIND9 at static Docker IP on the same bridge network
server=/amer.home/172.20.0.10
server=/merenda.home.arpa/172.20.0.10

# Cache settings
cache-size=10000
local-ttl=300
