# core stack

Contains: Technitium DNS, pgvector (PostgreSQL with vector extension), MongoDB.

## Version pinning — DO NOT UPDATE without explicit instruction

All images are digest- or tag-pinned. Do not bump versions without being asked.
These are core infrastructure services — an unplanned upgrade risks DNS outages
or data incompatibility.
