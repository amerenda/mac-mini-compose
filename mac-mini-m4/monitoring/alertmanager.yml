global:
  resolve_timeout: 5m

route:
  receiver: 'no-op'
  group_by: ['alertname', 'cluster']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h
  routes: []

receivers:
  - name: 'no-op'
