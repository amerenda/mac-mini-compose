# mosquitto stack

Standalone Mosquitto MQTT broker config (legacy). The active Mosquitto instance
is part of the `automation/` stack; this directory may be for reference or
alternate deployment.

## Version pinning — DO NOT UPDATE without explicit instruction

Do not change image tags without explicit instruction.
